@model List<Northwind.WebUI.Models.ProductResponseModel>
@{
    Layout = "_Layout";
}

<div class="row row-cols-1 row-cols-md-4 g-4">
    @foreach (var item in Model)
    {
        <div class="col">
            @await Html.PartialAsync("ProductListPartial",item)
        </div>
    }
</div>